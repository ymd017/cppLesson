質問１　
----------------------------------------------
C28251のエラーが出ています。
コンパイルはでき、想定通りの挙動はしています。
----------------------------------------------
警告が出ている箇所
↓のstrcat
char* strcat(char s1[], const char s2[]);
char* strcat(char s1[], const char s2[]) {}

質問２
----------------------------------------------
C6385のエラーが出ています。
コンパイルはでき、想定通りの挙動はしています。
----------------------------------------------
警告が出ている箇所
↓の条件式の部分
while (s1[i] != '\0') {
	i++;
}

for (int j = 0; s2[j] != '\0'; j++) {
	s1[i + j] = s2[j];
}
